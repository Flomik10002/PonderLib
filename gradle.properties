org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false
org.gradle.parallel=true
org.gradle.caching=true
# The Minecraft version must agree with the Forge version to get a valid artifact
minecraft_version=1.20.1
# The Forge version must agree with the Minecraft version to get a valid artifact
# https://files.minecraftforge.net/net/minecraftforge/forge/
forge_version=47.4.10
# ForgeGradle's own version range, not a Minecraft/Forge version
forgegradle_version=[6.0,6.2)
# The Mixin gradle plugin (mixingradle)'s own version range, not the Mixin library version itself
mixingradle_version=0.7.+
# The SpongePowered Mixin library version the mixingradle plugin resolves against
mixin_version=0.8.5
# The unique mod identifier for the mod. Must be lowercase in English locale. Must fit the regex [a-z][a-z0-9_]{1,63}
mod_id=ponderlib
# The human-readable display name for the mod.
mod_name=PonderLib
# The license of the mod. Review your options at https://choosealicense.com/. All Rights Reserved is the default.
mod_license=LGPL-3.0-or-later
# The mod version. See https://semver.org/. Starts below 1.0.0 on purpose: the public API has
# already changed shape several times during development (see docs/PLAN.md), so this signals "not
# yet API-stable" rather than implying a stability guarantee that hasn't been earned yet.
mod_version=0.7.1
# The group ID for the mod. It is only important when publishing as an artifact to a Maven repository.
# This should match the base package used for the mod sources.
# See https://maven.apache.org/guides/mini/guide-naming-conventions.html
mod_group_id=dev.flomik
# The authors of the mod. This is a simple text string that is used for display purposes in the mod list.
mod_authors=Flomik
# The description of the mod. This is a simple multiline text string that is used for display purposes in the mod list.
mod_description=An in-game documentation/tutorial system for Forge mods, letting any mod register \
    animated, block-accurate scenes for its own blocks and items.
